- 1 Processus qui cree et surveille les changements du file system tree et qui attend des requêtes msgq du genre :
 getMenuBar for user uid, retourne une addresse memoire partagee pour le menubar de l'utilisateur uid si possible maintient un compteur du nombre de connections de l'utilisateur.
 freeMenubar for user uid. Libere la shm.

 getFst for user uid with start path = p.  retourne une addresse memoire partagee pour le fst de l'utilisateur uid.
 freeFst for user uid. Libere la shm.

 getFstSearch for user uid and query q.  retourne une addresse memoire partagee pour le resultat de la recherche sur les items accessible par l'utilisateur uid.
 freeFstSearch for user uid. Libere la shm.

 isPathValid for user uid path p retourne true/false
